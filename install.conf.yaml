---
- defaults:
    link:
      create: true
      relink: true

- clean:
    '~'

- link:
    ~/.ssh:                                        '/keybase/private/andersonsilva/home/ssh'
    ~/.gitconfig:                                  'gitconfig'
    ~/.gitignore:                                  'gitignore'
    ~/.config/terminator/config:                   'terminator.cfg'
    ~/.zshrc:
      force: true
      path: 'zshrc'
